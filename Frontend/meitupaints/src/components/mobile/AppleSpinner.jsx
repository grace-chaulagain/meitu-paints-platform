// Shared home for the mobile primitives originally built in src/dealer/mobile/
// (per ADMIN_MOBILE_DESIGN_PROMPT.md §1) - a thin re-export, not a copy, so
// there is exactly one implementation and the dealer/dispatcher apps keep
// working unchanged.
export { AppleSpinner } from "../../dealer/mobile/AppleSpinner.jsx";
