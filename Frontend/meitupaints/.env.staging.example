# Staging frontend environment. Copy to Frontend/meitupaints/.env.staging if building the frontend separately.
# For same-origin Hostinger staging through the backend, leave VITE_API_BASE_URL empty.
VITE_API_BASE_URL=http://localhost:5002

# Public inquiry form submission endpoint. Leave blank to disable submissions.
VITE_INQUIRY_ENDPOINT=
